{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "title": "Metric Source Specification",
    "type": "object",
    "properties": {
        "metric": {
            "type": "string",
            "description": "The metric identifier this source specification is for"
        },
        "source": {
            "type": "string",
            "description": "The data source system (e.g., stripe, quickbooks, salesforce)"
        },
        "version": {
            "type": "string",
            "pattern": "^\\d+\\.\\d+\\.\\d+$",
            "description": "Semantic version of this source specification"
        },
        "url": {
            "type": "string",
            "format": "uri",
            "description": "API endpoint URL"
        },
        "filters": {
            "type": "object",
            "description": "Query filters to apply to the API call"
        },
        "formula": {
            "type": "string",
            "description": "Mathematical definition of how to calculate the metric from the API response"
        },
        "normalization": {
            "type": "object",
            "description": "Normalization rules for converting raw values to standard units"
        }
    },
    "required": ["metric", "source", "version", "url", "formula"],
    "additionalProperties": false
}